apply plugin: 'com.android.application'

android {
    signingConfigs {
        GPrelease {
            keyAlias 'timetableKey'
            keyPassword 'tyukl;pljmmj'
            storeFile file('D:/GooglePlay/ru_besuglovs_nu_timetable/ru_besuglovs_nu_timetable.jks')
            storePassword 'kl;sdfrdazza'
        }
    }
    compileSdkVersion 24
    buildToolsVersion "24.0.1"
    defaultConfig {
        applicationId "ru.besuglovs.nu.timetable"
        minSdkVersion 15
        targetSdkVersion 24
        versionCode 5
        versionName "0.8"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.GPrelease
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:24.2.0'
    compile 'com.squareup.retrofit:retrofit:1.8.0'
    compile 'com.google.code.gson:gson:2.4'
    compile 'com.j256.ormlite:ormlite-android:4.48'
}
